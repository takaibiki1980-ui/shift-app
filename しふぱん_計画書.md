# しふぱん 計画書
作成日: 2026-07-01  
ブランチ: phase5/night-balance  
最新コミット: 8db943d

---

## 1. アプリ概要

**しふぱん**（旧称：しふぽん）は介護・医療施設向けのシフト自動生成Webアプリ。

| 項目 | 内容 |
|---|---|
| フロントエンド | React 19 + Vite 8 |
| バックエンド | Supabase（認証・DB・ストレージ） |
| 主要ファイル | `src/App.jsx`（9,473行）、`src/shiftEngine.js`（1,650行）※未使用 |
| テスト | Vitest、9ファイル、131テスト（全通過） |
| デプロイ | Vite build → 静的ホスティング |

> **重要**: ブラウザが実行する `autoGenerate` は `src/App.jsx` 内（L1042）に直接定義されている。`src/shiftEngine.js` はインポートされておらず、ブラウザからは呼ばれていない。

---

## 2. 対応部署（デフォルト）

| 部署ID | 表示名 | シフト種別 |
|---|---|---|
| kaigo1 | 介護部 1階 | 早番・日勤・遅番・夜勤 |
| kaigo2 | 介護部 2階 | 早番・日勤・遅番・夜勤 |
| jimu | 事務所 | 日勤 |
| kango | 看護部 | 日勤 |
| eiyo | 栄養科 | 早番・日勤 |

役職制限例: 介護補助・介護助手 → 日勤のみ

---

## 3. 全機能一覧

### 3-1. 認証・アカウント

- メール／パスワード認証（Supabase Auth）
- 新規登録（施設名入力）
- ログイン・ログアウト
- スタッフポータル（QRコード・共有URLでスタッフ個人画面を表示）

### 3-2. シフト表示・編集

- 月間シフト表（カレンダー形式・横軸＝日付・縦軸＝スタッフ）
- セルの左クリック → シフト選択メニュー
- セルの右クリック → コンテキストメニュー（シフト変更・クリア）
- Excelからの貼り付け（TSVパース）
- ズーム機能（拡大・縮小）
- 祝日表示（@holiday-jp/holiday_jp）
- 勤務連続日数の警告表示
- 役職違反（役職外シフト）の警告表示

### 3-3. シフト自動生成

呼び出し経路:
```
ボタン onClick
 → handleGenerate() [App.jsx L8740]
 → _runGenerateCore() [App.jsx L8575]
 → bestOfN() [App.jsx L2980]  ← 30回試行・最良採用
 → autoGenerate() [App.jsx L1042]  ← 1回分生成
 → localSearchImprove() [App.jsx L2913]  ← 完成後swap最適化
```

**autoGenerate フェーズ順序**:

```
ステップ2: 夜勤配置（優先順位型スコアソート）
 ↓
Phase5 Step4-B: Night Slot Optimizer データ構築（res[]変更なし）
 ↓
enforceMaxStaff（maxStaff超過を強制修正）
 ↓
PassA: 休み日 → 確率的サンプリング配置
 ↓
PassB: 勤務日 → 全スタッフ統一処理（trend/deptAvg でサンプリング）
 ↓
PassC: 連続勤務超過の修正（PassA夜勤を保護）
 ↓
公休数調整①（休み過多→勤務変換 / 休み不足→休み追加）
 ↓
enforceMaxStaff ×複数回
 ↓
minStaff保証（最低配置人数を補充）
 ↓
enforceMaxStaff
 ↓
公休数回復フェーズ（日勤→休み変換）
 ↓
公休数超過バリデーション（休み→日勤変換）
 ↓
比率修復パス（shiftRatio目標に近づける）
 ↓
最終検証ログ
 ↓
return { shifts, warnings, timelineWarnings, diagnosticReport }
```

**bestOfN後 swap最適化（localSearchImprove）**:
- 完成シフト全体を対象に全スタッフ×全日ペアのswapを試行
- scoreShifts でスコア改善時のみswap採用
- 改善がなくなるまで最大3パス繰り返し
- swap対象外: 夜勤・明け・希望休・有休

**修正能力**:
| 能力 | 有無 |
|---|---|
| 勤務交換（swap） | ○ |
| 勤務移動（move） | × |
| 勤務再配置 | × |
| 局所探索 | ○ |
| 山登り法 | ○ |
| 評価改善ループ | ○ |
| 完成後の全体最適化 | ○ |

### 3-4. 傾向学習（learnedTrend）

- `computeLearnedTrend()` [App.jsx L503]: 過去シフトDB履歴からスタッフ別・曜日別・シフト別の出現確率を計算
- 例外月（手動修正が多い月）を除外できる
- PassB の確率サンプリングに使用

### 3-5. 希望休・有休管理

- スタッフ別・月別の希望休日指定（kiboByMonth）
- 有休日の指定（yukyuByMonth）
- 希望勤務リクエスト（shiftRequestsByMonth）
- 希望休の上限人数設定（kiboLimit）
- 一括公休設定モーダル（BulkKyukoModal）

### 3-6. 公休数（kyukoDays）管理

- スタッフ別・月別の目標公休数（kyukoDaysByMonth）
- デフォルト: 介護部 8日、栄養科 9日
- 公休数調整フェーズが自動生成後に修正

### 3-7. 診断レポート（DiagnosticEngine）

- Phase3〜4 で実装
- autoGenerate/bestOfN の戻り値に diagnosticReport を含む
- PassA/B/C/修正フェーズごとの変更セル数を記録
- topTrend（学習データとの一致率）を計測
- ブラウザコンソールに出力

### 3-8. エクスポート・共有

- CSV書き出し（buildCSV）
- 印刷用HTML生成（buildPrintHTML）
- Excel形式ダウンロード（xlsx-js-style）
- LINE共有（URLエンコード）
- QRコード表示（qrcode.react）
- Supabase経由でのシフト共有（shared_shifts テーブル）

### 3-9. 履歴・Undo

- Undo機能（undoStackRef、30履歴保持）
- Supabase shift_data_history テーブルへの保存
- 履歴一覧からの復元（ShiftHistoryModal）

### 3-10. 部署設定

- 部署の追加・編集・削除（DeptSettingModal）
- シフト種別のカスタム定義（カスタムシフト・baseType・色・時間）
- maxStaff（上限人数）設定
- minStaff（最低配置人数）設定
- 役職別シフト制限（roleShiftTypes）
- 最大連続勤務日数（maxConsecutive）
- 時間軸エンジン対応部署（requiredStart/requiredEnd）

### 3-11. スタッフ管理

- スタッフの追加・編集・削除（StaffModal）
- 役職・部署・夜勤可否・夜勤上限回数・shiftRatio設定
- スタッフポータル（個人向け画面、URLでアクセス）

### 3-12. 予定表（YoteiView）

- 日別の担当者配置（フロア配置表）
- 自動配置ルール設定（autoAssignDay）
- フロア設定モーダル（FloorSettingsModal）

### 3-13. 時間軸エンジン（generateTimeAxis）

- `src/lib/timeAxisEngine/` 以下に独立実装
- カバレッジエンジン（coverageEngine.js）
- 制約エンジン（constraintEngine.js）
- 学習エンジン（learningEngine.js）
- 緩和エンジン（relaxation.js）
- 対象部署: `engineType === 'time'` の部署のみ

### 3-14. データ永続化

- Supabase DB（クラウド、主要データ）
- localStorage（depts / staffList / shifts のローカルキャッシュ）

---

## 4. ファイル構成

```
shift-app/
├── src/
│   ├── App.jsx              # メイン（9,473行）UI + autoGenerate + bestOfN + localSearchImprove
│   ├── App.css              # スタイル
│   ├── shiftEngine.js       # ★App.jxsからインポートされていない（未使用）
│   ├── main.jsx             # エントリーポイント
│   ├── index.css
│   ├── lib/
│   │   ├── shiftAccessors.js
│   │   ├── validateShift.js
│   │   └── timeAxisEngine/
│   │       ├── index.js
│   │       ├── constraintEngine.js
│   │       ├── coverageEngine.js
│   │       ├── learningEngine.js
│   │       ├── relaxation.js
│   │       └── constraints/
│   │           ├── definitions.js
│   │           └── rules/（fairness / maxConsecutive / maxStaff / minInterval / nightSetPattern / requestLock / roleAllowedShifts / shiftRatio）
│   └── __tests__/（9ファイル・131テスト）
│       ├── constraintEngine.test.js
│       ├── coverageEngine.test.js
│       ├── learningEngine.test.js
│       ├── relaxation.test.js
│       ├── roleRestriction.test.js
│       ├── shiftAccessors.learning.test.js
│       ├── softRules.test.js
│       ├── timeAxisEngine.test.js
│       └── validateShift.golden.test.js
├── package.json
└── vite.config.js
```

---

## 5. 現時点の課題（実測値）

### 5-1. 公休違反（最重要課題）

**実測値（200試行平均 / kaigo1・kaigo2）**:

| フェーズ | kaigo1 公休違反人数 | kaigo2 公休違反人数 |
|---|---|---|
| PassB終了時点 | 0.555人 | 0.640人 |
| enforceMax①後 | +8.635増 | +8.510増 |
| 最終出力 | **7.300人/10人** | **7.380人/10人** |

→ スタッフ10人中7.3人が公休数目標未達または超過で終了している。

**発生源（確認済み）**: enforceMaxStaff①が公休違反の最大発生フェーズ（+8.6人/試行）。

### 5-2. 夜勤shortage（解決済み）

Step7-FA で PassC が PassA の夜勤配置を上書きする問題を修正。
`_nightProtectSnap` により PassC は PassA 夜勤セルをスキップ。
**現在: 夜勤shortage = 0.000**（200試行確認済み）。

### 5-3. src/shiftEngine.js は未使用

`src/shiftEngine.js` はブラウザから呼ばれていない。ブラウザの `autoGenerate` は `App.jsx` L1042 に直接実装されている。`shiftEngine.js` に追加したログはブラウザに出力されない。

---

## 6. 現在の作業内容

### 作業ブランチ

```
phase5/night-balance
最新コミット: 8db943d
```

### 実施済み作業（Step7シリーズ）

| Step | 内容 | 状態 |
|---|---|---|
| Step7-FA | PassC 夜勤保護（_nightProtectSnap）実装・実測 | 採用・マージ済み |
| Step7-FA正式 | debugProtectNightフラグ削除・常時有効化 | 完了 |
| Step7-G | 公休違反発生フェーズ計測 | 完了（enforceMax①が最大発生源と確定） |
| Step7-H | enforceMax① sortKyukoFirst試み | 不採用（有意に悪化） |
| Step7-I | ブラウザコンソールへのフェーズログ追加（[シフトログ]） | 実装済み（shiftEngine.js）★未表示 |
| Step7-I追加 | enforceMax超過バリデーションログ（[超過バリデーション]） | 実装済み（shiftEngine.js）★未表示 |
| [KYUKO]ログ | 公休数調整①②にKYUKO-START/TARGET/SKIP/FIX/ENDログ追加 | 実装済み（shiftEngine.js）★未表示 |

★ 上記ログは全て `shiftEngine.js` に実装したが、ブラウザは `App.jsx` の `autoGenerate` を呼ぶため、**ブラウザコンソールには出力されない**。

### 現在判明した問題

- **ブラウザが読むコードと編集しているコードが別ファイル**
  - 編集: `src/shiftEngine.js`
  - ブラウザ実行: `src/App.jsx` 内の `autoGenerate`（L1042）
  - `shiftEngine.js` は `App.jsx` から import されていない

### 次に必要な判断（ユーザー確認待ち）

以下のどちらかを選択する必要がある:

**選択肢A**: ログを `App.jsx` の `autoGenerate`（L1042〜）に移植する
- ブラウザで実際に動いているコードにログを追加できる
- 実機でログが確認できる

**選択肢B**: `src/shiftEngine.js` を `App.jsx` から import し直す（統合作業）
- 大規模なリファクタリングが必要
- 現在のフェーズ方針（ログ確認優先）と合わない

---

## 7. 開発方針・制約ルール（確定）

1. **推測禁止・改善案禁止** — コード・ログ・実測結果だけで判断
2. **検証コードは「使う→実測→採用・不採用→削除」を1セット**
3. **autoGenerate / bestOfN / computeLearnedTrend への変更禁止**
4. **PassA / PassB / PassC 変更禁止**
5. **1改善 = 1ブランチ = 1PR = 1効果検証 = 1マージ**
6. **改善確認できない場合はPush・PR・mainマージ禁止**
7. **mainへのマージ禁止（phase5/night-balance ブランチのみ作業）**
8. **ログ確認が終わるまでコード変更禁止・次Phase禁止**

---

## 8. テスト構成

```
テストファイル: 9本
総テスト数: 131（全通過）

constraintEngine.test.js    - 制約エンジン
coverageEngine.test.js      - カバレッジエンジン
learningEngine.test.js      - 学習エンジン
relaxation.test.js          - 緩和エンジン
roleRestriction.test.js     - 役職制限（Excelノイズ耐性・2-optスワップ拒否）
shiftAccessors.learning.test.js - シフトアクセサー
softRules.test.js           - ソフト制約
timeAxisEngine.test.js      - 時間軸エンジン統合
validateShift.golden.test.js - ゴールデンテスト
```

実行コマンド: `npx vitest run`

---

## 9. 主要依存ライブラリ

| ライブラリ | バージョン | 用途 |
|---|---|---|
| react | 19.2.4 | UI |
| @supabase/supabase-js | 2.103.0 | 認証・DB |
| lucide-react | 1.17.0 | アイコン |
| qrcode.react | 4.2.0 | QRコード生成 |
| xlsx-js-style | 1.2.0 | Excel出力 |
| @holiday-jp/holiday_jp | 2.5.1 | 日本祝日判定 |
| vite | 8.0.4 | ビルド |
| vitest | 4.1.6 | テスト |
| playwright | 1.60.0 | E2Eテスト（設定済み） |

---

## 10. 今後の改善候補（未着手・推測禁止のため効果未確認）

以下は現時点でコードから確認できる「改善を検討できる箇所」のリスト。
効果は実測するまで不明。

- enforceMaxStaff① の公休違反発生メカニズムの解明（要ログ実機確認）
- 公休数調整①の SKIP ケースが何件あるかの計測（要ログ実機確認）
- App.jsx の autoGenerate にログを直接追加して実機確認を可能にする

---

*以上*
